package com.morak.member.repository;

import com.morak.member.entity.MediaConsent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MediaConsentRepository extends JpaRepository<MediaConsent, Long> {
}
